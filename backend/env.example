# Backend Environment Variables
# Copy these to your deployment platform (Vercel, Render, etc.)

# ===========================================
# REQUIRED VARIABLES
# ===========================================

# MongoDB Connection String (from MongoDB Atlas)
# Format: mongodb+srv://username:password@cluster.mongodb.net/database_name?retryWrites=true&w=majority
MONGODB_URL=mongodb+srv://your_username:your_password@cluster0.xxxxx.mongodb.net/leetcode_leaderboard?retryWrites=true&w=majority

# Admin Token for protected endpoints (choose a strong secret)
ADMIN_TOKEN=your-super-secret-admin-token-change-me

# ===========================================
# CORS CONFIGURATION
# ===========================================

# Allowed frontend origins (update with your Vercel URL)
# Format: JSON array of URLs
CORS_ORIGINS=["https://your-app.vercel.app"]

# ===========================================
# OPTIONAL VARIABLES (defaults shown)
# ===========================================

# Database name (usually included in MONGODB_URL)
# MONGODB_DB_NAME=leetcode_leaderboard

# Rate limit delay between LeetCode API calls (seconds)
# RATE_LIMIT_DELAY=1.0

# Auto-update interval in hours
# REFRESH_INTERVAL_HOURS=1

# App metadata
# APP_NAME=LeetCode Leaderboard API
# APP_VERSION=1.0.0

